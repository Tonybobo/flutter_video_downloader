import 'package:flutter/material.dart';

class TaskDownlodingScreen extends StatelessWidget {
  const TaskDownlodingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Task Downloading Screen"),
    );
  }
}
